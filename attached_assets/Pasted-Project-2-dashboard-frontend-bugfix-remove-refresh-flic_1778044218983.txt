Project 2 dashboard frontend bugfix — remove refresh flicker/glitch from Inbox, Channels, and Escalations.

Important current architecture:
- Project 2 Replit is frontend only.
- api.unboks.org points to the canonical Python backend on Vultr.
- Do not modify artifacts/api-server.
- Do not modify backend logic, DNS, secrets, deployment, auth, or Python API assumptions.
- Work only in artifacts/unboks frontend.
- Do not touch Project 1 or Project 3.
- Do not commit.
- Do not publish until reviewed.

Bug:
Every time the user refreshes https://dashboard.unboks.org/, there is a very fast visual glitch/flicker in:
- Inbox
- Channels sidebar counts/list
- Escalations

It looks like old messages or fallback data flashes for a split second before the real API data loads.

Likely cause:
The frontend is rendering mock/fallback data or stale default data during the initial loading state, then replacing it with live Python API data.

Task:
Audit and fix the initial-load rendering so no fake/stale messages flash on page refresh.

Required behavior:
1. On refresh, do NOT render mock conversations while live data is loading.
2. Do NOT render old fallback data in Inbox.
3. Do NOT render stale channel counts.
4. Do NOT render stale escalation counts.
5. Show a clean loading state/skeleton while API data is loading.
6. Once API data arrives, render real data.
7. If API fails, show a calm error/empty state, not mock/demo data.
8. Unknown/missing data should not create fake messages.
9. Do not break local task fallback behavior; this bug is about Inbox/Channels/Escalations.
10. Do not remove mock files globally unless safe; just stop using them as live fallback.

Likely files:
- artifacts/unboks/src/pages/Inbox.tsx
- artifacts/unboks/src/components/inbox/DashboardShell.tsx
- artifacts/unboks/src/components/inbox/Drawer.tsx
- artifacts/unboks/src/components/inbox/MessageRow.tsx
- artifacts/unboks/src/hooks/use-client-api.ts
- artifacts/unboks/src/lib/conversation-mapper.ts
- artifacts/unboks/src/data/conversations.ts

Investigation:
- Search for imports from artifacts/unboks/src/data/conversations.ts.
- Check whether Inbox uses MOCK when conversations are undefined/loading.
- Check whether DashboardShell uses MOCK for channelCounts/escalationsCount.
- Check whether Drawer receives mock counts before API loads.
- Check whether React Query keepPreviousData / placeholderData is causing stale flash.
- Check whether enabled channels or filters initialize with stale conversation data.
- Check whether Escalations uses mock fallback when API not loaded.

Preferred fix:
- Use [] while loading live conversations/escalations.
- Add isLoading states and simple skeleton rows.
- Keep previous data only if it is the same user/session and not mock.
- Remove any fallback to imported MOCK conversations for live dashboard rendering.
- Keep MOCK only for development/demo if still needed, but never on production dashboard initial load.
- If there is a development preview fallback, gate it explicitly behind import.meta.env.DEV and not production.

Visual loading state:
- Inbox list: 3 subtle skeleton rows or “Loading conversations…”
- Sidebar counts: hide counts or show 0 until real data loads
- Escalations count: hide count or show 0 until real data loads
- No flashing fake names/messages

Acceptance:
- Refresh dashboard.unboks.org and no old/fake messages flash.
- Inbox stays stable while loading.
- Channels counts do not flash wrong values.
- Escalations count does not flash wrong values.
- Real API data still renders after load.
- WhatsApp filter still shows WhatsApp conversations.
- Email filter still shows Email conversations.
- Inbox reset still works.
- No backend changes.
- No artifacts/api-server changes.
- No .replit changes.
- Build/typecheck clean.
- Report exact files changed.
- Do not commit.
- Do not publish.