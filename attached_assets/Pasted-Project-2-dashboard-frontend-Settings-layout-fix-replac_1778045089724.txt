Project 2 dashboard frontend — Settings layout fix: replace category sidebar with top tabs.

Important current architecture:
- Project 2 Replit is frontend only.
- api.unboks.org points to the canonical Python backend on Vultr.
- Do not modify artifacts/api-server.
- Do not modify backend logic, DNS, secrets, deployment, auth, or Python API assumptions.
- Work only in artifacts/unboks frontend.
- Do not touch Project 1 or Project 3.
- Do not commit.
- Do not publish until reviewed.

Issue:
The redesigned Settings page wastes too much horizontal space.
The left Settings category sidebar creates a large empty column and makes the actual settings content too narrow.

Also, the page shows “Settings” twice:
- once as the dashboard/shell page label
- once as the big internal page title

This feels messy and not premium.

Task:
Update the Settings page layout.

Required changes:
1. Remove the left category sidebar inside Settings.
2. Replace it with a horizontal top tab bar.
3. Keep the same categories:
   - Workspace
   - Your Info
   - Channels
   - Escalation Alerts
   - Labels & Preferences
4. Top tab bar should sit under the Settings page title/subtitle.
5. Top tab bar should be clean, premium, and horizontally scrollable on mobile if needed.
6. Content area should use the available page width better.
7. Reduce wasted white space.
8. Do not change any Settings functionality.
9. Do not remove any sections.
10. Do not touch Inbox, Tasks, DashboardShell, or backend.

Duplicate Settings label:
- If there is a small “Settings” breadcrumb/page label above the main title, remove or hide it for this page if possible.
- The page should show only one clear main title:
  Settings
- Subtitle:
  Manage your workspace, AI information, channels, and alerts.
- Do not create two visible Settings headings.

Desktop layout:
- Use a centered/wide content area.
- Recommended max width: around 1100–1200px.
- Top tabs full width.
- Active tab highlighted with blue accent and soft background or underline.
- Settings card/content below should be wider than now.

Mobile layout:
- Top tabs can scroll horizontally.
- No horizontal page overflow.
- Content cards full width.
- Comfortable spacing.

Design:
- Premium SaaS look.
- White/soft grey background.
- Subtle borders.
- No giant empty left column.
- No heavy shadows.
- Clean hierarchy.

Files likely involved:
- artifacts/unboks/src/pages/Settings.tsx

Do not touch:
- artifacts/api-server
- DashboardShell unless absolutely necessary
- Inbox
- Tasks
- auth
- backend
- DNS
- deployment config
- .replit
- attached_assets

Acceptance:
- Settings category navigation is now a top tab bar, not a left sidebar.
- No large wasted left-side empty space.
- Content uses page width better.
- Only one visible “Settings” title remains.
- All five categories still work.
- Workspace still works.
- Your Info still works.
- Channels still works.
- Escalation Alerts still works.
- Labels & Preferences still works.
- Mobile layout is clean.
- No horizontal overflow.
- Build/typecheck clean.
- Report exact files changed.
- Do not commit.
- Do not publish.