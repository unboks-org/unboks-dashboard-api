Project 2 dashboard frontend — redesign Tasks page ONLY using Refero/Repro references.

Important current architecture:
- Project 2 Replit is frontend only.
- api.unboks.org points to the canonical Python backend on Vultr.
- Do not modify artifacts/api-server.
- Do not modify backend logic, DNS, secrets, deployment, auth, or Python API assumptions.
- Work only on the Tasks page and task components.
- Do not touch Settings.
- Do not touch Inbox.
- Do not touch DashboardShell unless absolutely required.
- Do not touch Project 1 or Project 3.
- Do not commit.
- Do not publish until reviewed.

Task:
Redesign ONLY the /tasks page.

Use Refero/Repro:
Use real SaaS references before redesigning.
References should be from:
- Linear issue list
- Notion task cards
- Slack saved items/tasks
- Stripe dashboard spacing
- Vercel clean settings/product UI

If Refero/Repro is not connected, stop and report that clearly before redesigning.

Current problem:
The Tasks page is too raw.
It needs to feel like a premium, simple, shared task board for Calvin and Jr.
Nothing fancy, but it must look sharp, calm, organized, and usable.

Purpose:
Calvin and Jr work remotely.
They need one place to post tasks, text, and screenshots for each other.
Tasks can be marked done.
Backend is not connected yet, so local pending tasks must be clearly marked.

Route:
/tasks

Also support:
/Tasks → same page or redirect to /tasks if already implemented. Do not overwork routing.

Design goal:
A premium but simple two-person task board.

Think:
- Linear issue list simplicity
- Notion clean task cards
- Slack calm readability
- Stripe dashboard spacing

Page title:
Tasks

Subtitle:
Shared task board for Calvin and Jr.

Important behavior:
- Do not pretend tasks are already shared if backend is missing.
- If backend unavailable, allow local pending tasks.
- Clearly show:
  “Saved locally — will sync when backend is connected.”
- Local tasks must persist after refresh.
- Backend tasks and local pending tasks should be visually distinguishable.

Tasks layout desktop:
- Header with title + small backend/sync status pill
- Composer card at top
- Filter tabs: Open / Done / All
- Task list below
- Optional small info strip, not a huge scary error:
  “Shared tasks are not connected yet. You can still create local pending tasks.”

Tasks layout mobile:
- Single column
- Composer first
- Filter tabs near top
- Large readable cards
- Big Mark done button
- Screenshot paste/upload must not break mobile

Task composer:
- To selector:
  - Jr
  - Calvin
- Rich text / textarea area
- Screenshot paste/upload support
- Clear paste hint:
  “Paste screenshots or attach images.”
- Button:
  “Add task”
- If backend unavailable:
  small calm note:
  “Tasks will be saved locally until sync is available.”

Task cards:
Each card should show:
- assigned person
- created by
- created date/time
- status: Open / Done
- task body
- screenshot thumbnails if attached
- Pending sync badge if local-only
- Mark done button
- Reopen button for done tasks

Visual requirements:
- Clean white/soft grey page
- Premium SaaS spacing
- Subtle borders
- Rounded cards, 12–16px
- No heavy shadows
- No giant red backend error banner
- Open tasks feel active
- Done tasks visually softer
- Pending sync badge visible but calm
- Buttons clear and aligned
- No clutter
- No technical/debug language

Screenshot support:
- User can paste screenshots into composer.
- User can attach/select images.
- Thumbnails appear before saving.
- User can remove attached screenshots before saving.
- Saved local task shows thumbnails.
- If real upload backend is unavailable, screenshots are local-only and clearly marked.
- Max 5 images per task.
- Max 10 MB per image.
- Accepted:
  - PNG
  - JPG/JPEG
  - WebP

Tasks states:
1. Backend connected:
   - show shared tasks
   - allow create/update through API

2. Backend missing:
   - show local pending mode
   - allow local add/done/reopen
   - show pending sync badges
   - show calm status, not scary error

3. Backend returns error:
   - do not kill page
   - preserve local tasks
   - show calm status

Copy:
Empty Open:
“No open tasks.”

Empty Done:
“No completed tasks yet.”

Composer placeholder:
“Write a task…”

Sync info:
“Saved locally — will sync when backend is connected.”

Backend missing:
“Shared tasks are not connected yet. You can still create local pending tasks.”

Done button:
“Mark done”

Reopen:
“Reopen”

Do not:
- Touch Settings.
- Touch Inbox.
- Touch backend.
- Touch artifacts/api-server.
- Build comments.
- Build deadlines.
- Build priority.
- Build kanban.
- Build drag/drop.
- Build AI.
- Overengineer.
- Use localStorage as final shared source of truth.
- Pretend Jr can see local-only tasks before backend sync.

Files likely involved ONLY:
- artifacts/unboks/src/pages/Tasks.tsx
- artifacts/unboks/src/components/tasks/TaskCard.tsx
- artifacts/unboks/src/components/tasks/TaskComposer.tsx
- artifacts/unboks/src/lib/tasks-api.ts
Possibly:
- artifacts/unboks/src/hooks/use-local-pending-tasks.ts if it already exists or is needed

Do not touch:
- artifacts/unboks/src/pages/Settings.tsx
- artifacts/unboks/src/pages/Inbox.tsx
- artifacts/unboks/src/components/inbox/*
- artifacts/api-server/*
- .replit
- attached_assets

Implementation:
1. Inspect only Tasks page/task components/tasks-api.
2. Use Refero/Repro references.
3. Redesign Tasks frontend only.
4. Preserve existing task API contract and local pending fallback.
5. Run typecheck/build.

Acceptance:
- /tasks looks premium and organized.
- /tasks loads even if backend missing.
- Local pending tasks can be added.
- Local pending tasks persist after refresh.
- Pending sync state is clear and not scary.
- Screenshot paste/upload UX is clean.
- Task cards are readable and professional.
- Mobile layout is excellent.
- Settings unchanged.
- Inbox unchanged.
- No backend changes.
- No artifacts/api-server changes.
- No .replit changes.
- No attached_assets changes.
- Build/typecheck clean.
- Report exact files changed.
- Report whether Refero/Repro was connected and what references were used.
- Do not commit.
- Do not publish.