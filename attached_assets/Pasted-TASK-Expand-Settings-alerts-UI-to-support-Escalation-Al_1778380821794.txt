TASK: Expand Settings alerts UI to support Escalation Alerts and Appointment Alerts.

PROJECT SCOPE:
Project 2 customer dashboard only.
Frontend only.
Do NOT touch Project 1 public website.
Do NOT touch pricing.
Do NOT modify backend/API implementation.
Do NOT modify artifacts/api-server.
Do NOT change alert delivery logic.
Do NOT commit.
Do NOT publish until reviewed.

PROBLEM:
Settings currently has only “Escalation Alerts”.

But operators also need notifications when a booking/appointment is created or confirmed.

We need the Settings UI to clearly support:
1. Escalation alerts
2. Appointment alerts

Both should use the configured notification channels:
- Email default
- Alternative email
- WhatsApp
- Telegram
- Messenger

GOAL:
Create a clear alerts settings area where the user can configure where they receive:
- urgent escalation notifications
- appointment/booking notifications

CURRENT UI:
Title:
Escalation Alerts

Subtitle:
Choose where urgent escalation alerts should be sent.

This is too narrow.

REQUIRED NEW STRUCTURE:

Rename main section title to:

Alerts

Subtitle:
Choose where notifications should be sent.

Inside the card, create two alert types:

1. Escalation alerts
Description:
Get notified when Marina needs human help or a customer conversation requires attention.

2. Appointment alerts
Description:
Get notified when a customer confirms an appointment, booking, order, or scheduled call.

Use compact premium design.
No unnecessary vertical whitespace.
Do not make this bulky.

CHANNELS:

Keep the existing channel configuration UI:
- Email default
- Alternative email
- WhatsApp
- Messenger
- Telegram

But make it clear these destinations can receive both escalation and appointment alerts.

Required email row:
Email
Always on, sent to [default email]
Alternative: [alternative email if set]
Default badge

Alternative email input remains.

Required channel rows:
WhatsApp
Messenger
Telegram

Keep existing:
- enabled toggle
- destination input
- status badge:
  Active
  Provider not connected
  Saved only
  Failed

ALERT TYPE TOGGLES:

Add compact toggles for alert types.

Preferred layout:

Alert types
[on] Escalation alerts
[on] Appointment alerts

or:

Send these alerts:
[x] Escalations
[x] Appointments

Default:
- Escalation alerts ON
- Appointment alerts ON

If user turns Appointment alerts off:
- Keep destinations saved
- Backend should not send appointment alerts when disabled

If user turns Escalation alerts off:
- If product requires escalation email always on, then disable this toggle or show:
  “Escalation email is always on”
- Otherwise allow turning off non-email channels only.

Be honest with current backend:
If backend does not support appointment alert preference yet, show saved UI but do not claim delivery works unless backend confirms.

COPY REQUIREMENTS:

Use:
“Alerts”
“Escalation alerts”
“Appointment alerts”
“Alternative email”
“Send appointment alerts when a booking or appointment is confirmed.”

Do NOT use:
“webhook”
“backend”
“API”
“database”
“notification_type”
“cron”

BACKEND SHAPE TO PREPARE FOR:

GET /api/{client}/dashboard/api/settings/alerts
or existing:
GET /api/{client}/dashboard/api/settings/escalation-alerts

The frontend should remain compatible with the existing escalation-alerts endpoint, but structure code so Jr can extend it.

Suggested frontend model:

{
  "alertTypes": {
    "escalations": true,
    "appointments": true
  },
  "channels": {
    "email": {
      "enabled": true,
      "destination": "default",
      "resolvedDestination": "butlerbensonagent@gmail.com",
      "alternativeDestination": "calvin@gaimin.io",
      "deliveryStatus": "active"
    },
    "whatsapp": {
      "enabled": true,
      "destination": "+351963618003",
      "deliveryStatus": "active"
    },
    "telegram": {
      "enabled": true,
      "destination": "@calvinadamus",
      "deliveryStatus": "provider_not_configured"
    },
    "messenger": {
      "enabled": false,
      "destination": "",
      "deliveryStatus": "provider_not_configured"
    }
  }
}

If backend still returns old shape:
- Keep existing behavior.
- Default appointment alerts UI to ON locally.
- Show no fake success for delivery.

DESIGN:
Premium compact settings panel.
Use rows, not giant cards.
Good alignment.
Small status badges.
No excessive vertical spacing.
Mobile clean.
No horizontal overflow.

LIKELY FILES:
- artifacts/unboks/src/pages/Settings.tsx
- artifacts/unboks/src/hooks/use-escalation-notification-preferences.ts
- artifacts/unboks/src/lib/api.ts

Possibly rename hook later, but do not overbuild:
- use-alert-notification-preferences.ts

ACCEPTANCE:
- Main section title says “Alerts”.
- UI shows Escalation alerts and Appointment alerts.
- Appointment alerts can be enabled/disabled in UI.
- Existing email default remains visible.
- Alternative email remains visible.
- WhatsApp destination remains visible.
- Telegram and Messenger provider-not-connected status remains honest.
- Save still works for existing escalation alert settings.
- No backend/API code changed.
- Build/typecheck clean.

REPORT BACK:
1. Files changed.
2. How Escalation alerts and Appointment alerts are shown.
3. Whether existing escalation alert endpoint is still used.
4. Whether appointment alerts are UI-prepared only or actually saved through backend shape.
5. Confirmation no backend/API implementation changed.
6. Confirmation Project 1 was not touched.

Do not commit.
Do not publish until reviewed.