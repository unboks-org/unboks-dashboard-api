Project 2 dashboard frontend — add Copy text action to Task cards.

Important current architecture:
- Project 2 Replit is frontend only.
- api.unboks.org points to the canonical Python backend on Vultr.
- Do not modify artifacts/api-server.
- Do not modify backend logic, DNS, secrets, deployment, auth, or Python API assumptions.
- Work only on Tasks page/task components.
- Do not touch Settings unless absolutely necessary.
- Do not touch Inbox.
- Do not touch DashboardShell.
- Do not touch Project 1 or Project 3.
- Do not commit.
- Do not publish until reviewed.

Issue:
Saved task cards have Edit and Mark done actions, but no way to quickly copy the full task text.

Task:
Add a “Copy text” action to each saved task card.

Required behavior:
1. Add a Copy text button next to Edit in the task card action/footer area.
2. Button should copy the task body text to clipboard.
3. It should copy clean plain text, not HTML tags.
4. Preserve line breaks.
5. Do not include metadata like Calvin → Jr, timestamp, Pending sync, or buttons.
6. If task has bodyText, copy bodyText.
7. If only bodyHtml exists, convert/sanitize to plain text before copying.
8. After copying, show a small success state:
   - button text changes to “Copied”
   - or a subtle confirmation appears
   - reset after ~1.5 seconds
9. If clipboard API fails, show a calm error:
   - “Could not copy”
10. Button should be placed near Edit, before Mark done.
11. Mobile layout must remain clean.
12. Do not change task saving/sync behavior.

Preferred UI:
Footer actions:
- Copy text
- Edit
- Mark done

or if space is tight:
- Copy
- Edit
- Mark done

Use icon if already available:
- Copy icon
- Pencil/Edit icon
- Check icon

Likely file:
- artifacts/unboks/src/components/tasks/TaskCard.tsx

Possibly:
- artifacts/unboks/src/pages/Tasks.tsx only if action handlers live there.

Do not touch:
- artifacts/api-server
- backend
- DNS
- auth
- deployment config
- .replit
- attached_assets

Acceptance:
- Each task card has Copy text next to Edit.
- Clicking Copy text copies the full task text.
- Copied text preserves paragraphs/line breaks.
- Copied text does not include UI metadata.
- Button shows success feedback.
- Edit still works.
- Mark done still works.
- Mobile still looks good.
- Build/typecheck clean.
- Report exact files changed.
- Do not commit.
- Do not publish.