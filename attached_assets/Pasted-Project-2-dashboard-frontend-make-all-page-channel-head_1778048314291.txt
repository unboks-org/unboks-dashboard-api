Project 2 dashboard frontend — make all page/channel headers consistent using Refero/Repro.

Important current architecture:
- Project 2 Replit is frontend only.
- api.unboks.org points to the canonical Python backend on Vultr.
- Do not modify artifacts/api-server.
- Do not modify backend logic, DNS, secrets, deployment, auth, or Python API assumptions.
- Work only in artifacts/unboks frontend.
- Do not touch backend.
- Do not touch Project 1 or Project 3.
- Do not commit.
- Do not publish until reviewed.

Task:
Fix inconsistent top/page headers across dashboard views.

Use Refero/Repro:
Use real SaaS dashboard header references before redesigning.
References should include:
- Linear
- Intercom inbox
- Front inbox
- Stripe dashboard
- Slack workspace
If Refero/Repro is not connected, stop and report clearly before editing.

Current issue:
The top header/page title area changes size and typography between views.
Examples:
- Inbox has title + subtitle and a compact search field.
- WhatsApp has a dot + title + subtitle but spacing differs.
- Email/Instagram are inconsistent.
- Escalations title is different size/weight/height.
- Bookings title is different.
When browsing between sections, the whole top bar appears to jump.

Goal:
Make the top/page header static and consistent across all dashboard pages and channel views.

Use Inbox as the base standard.

Required standard header dimensions:
1. Header area must have the same height across:
   - Inbox
   - WhatsApp
   - Email
   - Instagram
   - Facebook
   - Messenger
   - X
   - TikTok
   - Escalations
   - Bookings/Orders
   - Analytics if present
   - Settings if this header appears there
   - Tasks if this header appears there
2. Use the Inbox header as the base:
   - same title font size
   - same title font weight
   - same subtitle font size
   - same vertical padding
   - same alignment
   - same search placement when search is present
3. No page should have a bigger or smaller header that causes layout jump.
4. If a page has no subtitle, reserve the same subtitle line height or show a short useful subtitle.
5. If a page does not use search, reserve layout cleanly so content does not jump awkwardly, or keep right side empty with same header height.
6. Channel pages should use the same title/subtitle structure:
   - WhatsApp
   - 3 conversations
   - optional small dot/icon inline, but it must not change height.
7. Escalations should use:
   - Escalations
   - Conversations that need your attention
8. Bookings/Orders should use:
   - [dynamic label]
   - Customer bookings and orders
9. Settings should use:
   - Settings
   - Manage your workspace, AI information, channels, and alerts.
10. Tasks should use:
   - Tasks
   - Shared task board for Calvin and Jr.
11. Do not show duplicate page labels.

Search:
- On Inbox/channel views, search should be on the right side on desktop.
- Search should have the same size and placement on all inbox/channel views.
- Search should not appear huge.
- Search should not cause vertical jump.
- If search is not available on a page, do not render a different-height header.

Visual rules:
- Title font: use Inbox title size/weight as source of truth.
- Subtitle font: use Inbox subtitle size/color as source of truth.
- Header padding: same everywhere.
- Header border/background: same everywhere.
- Keep premium SaaS style.
- No overdesign.

Likely files:
- artifacts/unboks/src/components/inbox/DashboardShell.tsx
- artifacts/unboks/src/components/inbox/Header.tsx
- artifacts/unboks/src/pages/Inbox.tsx
- artifacts/unboks/src/pages/Bookings.tsx
- artifacts/unboks/src/pages/Settings.tsx
- artifacts/unboks/src/pages/Tasks.tsx
- artifacts/unboks/src/pages/Analytics.tsx if present

Important investigation:
- Find where each page title/header is rendered.
- Identify duplicate title rendering.
- Consolidate into one reusable header pattern if possible.
- Avoid page-specific title sizes.
- Make channel views pass title/subtitle into the same header component.
- Do not break search functionality.
- Do not break channel filtering.
- Do not break sidebar active state.

Acceptance:
- Switching between Inbox, WhatsApp, Email, Escalations, Bookings, Settings, Tasks does not cause top header height jump.
- All titles use the same font size/weight as Inbox.
- All subtitles use the same font size/color as Inbox.
- Header dimension is consistent.
- Search stays compact and aligned.
- WhatsApp shows “WhatsApp / 3 conversations”.
- Email shows “Email / 1 conversation”.
- Inbox shows “Inbox / All conversations”.
- Escalations shows “Escalations / Conversations that need your attention”.
- Bookings uses dynamic label and matching subtitle.
- No duplicate top titles.
- Build/typecheck clean.
- Report exact files changed.
- Report whether Refero/Repro was connected and what references were used.
- Do not commit.
- Do not publish.