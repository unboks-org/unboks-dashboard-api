Project 2 dashboard frontend edit #6 — fix mobile horizontal overflow on Settings.

Important current architecture:
- Project 2 Replit is frontend only.
- api.unboks.org points to the canonical Python backend on Vultr.
- Do not modify artifacts/api-server.
- Do not modify backend logic, DNS, secrets, deployment, or Python API assumptions.
- Work only in artifacts/unboks frontend.
- Do not touch Project 1 or Project 3.
- Do not commit.
- Do not publish.

Issue:
On mobile Settings page, the page can slide left/right horizontally.
There is horizontal overflow.
The screenshot shows the long API endpoint value in Client & System likely causing the page to become wider than the viewport.

Task:
Fix the Settings mobile layout so the page is stable and never scrolls horizontally.

Required behavior:
1. No horizontal scrolling/sliding on mobile.
2. Settings content must stay within viewport width.
3. Long values like API endpoint must wrap, truncate, or be contained.
4. The API endpoint should not push the page wider.
5. Client & System rows should be mobile-safe.
6. Source of Truth/Your Info cards should not overflow either.
7. Keep desktop layout clean.
8. Do not redesign the full page.

Preferred fix:
- In Settings.tsx, make long code/value pills use:
  - max-width: 100%
  - min-width: 0
  - overflow-hidden
  - text-overflow: ellipsis
  - whitespace normal or break-all where appropriate
- For API endpoint specifically, allow line break or ellipsis.
- Ensure Settings container uses overflow-x-hidden if appropriate.
- Ensure any row with label + value uses min-w-0 on the value side.
- Avoid fixed widths that exceed mobile viewport.

Likely file:
- artifacts/unboks/src/pages/Settings.tsx
Possibly:
- artifacts/unboks/src/index.css only if there is a global overflow issue, but prefer local fix.

Acceptance:
- On mobile Settings page, no left-right sliding.
- No horizontal scrollbar/overflow at bottom.
- API endpoint no longer expands past screen.
- Text remains readable or safely truncated.
- Desktop layout still looks clean.
- Build/typecheck clean.
- Report exact files changed.
- Do not commit.
- Do not publish.