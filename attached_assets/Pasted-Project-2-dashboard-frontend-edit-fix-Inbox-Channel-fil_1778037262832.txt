Project 2 dashboard frontend edit — fix Inbox/Channel filtering and show message source.

Important current architecture:
- Project 2 Replit is frontend only.
- api.unboks.org points to the canonical Python backend on Vultr.
- Do not modify artifacts/api-server.
- Do not modify backend logic, DNS, secrets, deployment, or Python API assumptions.
- Work only in artifacts/unboks frontend.
- Do not touch Project 1 or Project 3.
- Do not commit.
- Do not publish.

Issue:
The Inbox shows 3 conversations that are WhatsApp messages.
But when clicking the WhatsApp channel in the sidebar, those same conversations do not show under WhatsApp.
This is wrong.

Also:
- Message rows do not clearly show where each message came from.
- WhatsApp messages should show a small WhatsApp icon/name or badge.
- Inbox should show all conversations from all channels.
- Channel filters should show only that channel.
- When clicking another channel, the Inbox sidebar button becomes unresponsive or does not properly reset the filter.

Required behavior:
1. Inbox view:
   - Shows all conversations from all channels.
   - No channel filter active.
   - Sidebar Inbox button must always reset back to all conversations.

2. WhatsApp channel:
   - Shows only WhatsApp conversations.
   - The 3 WhatsApp conversations currently visible in Inbox must also appear when WhatsApp is selected.
   - Sidebar WhatsApp row should become selected/active when filtering by WhatsApp.

3. Other channels:
   - Email shows only Email.
   - Instagram shows only Instagram.
   - Facebook shows only Facebook.
   - Messenger shows only Messenger.
   - Unknown/missing channel must not become Email.
   - Unknown can remain visible in Inbox/All only.

4. Message rows:
   - Each conversation row must visibly show its source channel.
   - For WhatsApp messages, show a small WhatsApp icon and label “WhatsApp”.
   - For Email, show Email.
   - For Instagram, Facebook, Messenger, etc., show their label/icon.
   - Keep the row clean and not cluttered.
   - Prefer a small channel badge near the sender/title or preview.
   - Do not replace the contact name; channel is extra metadata.

5. Click behavior:
   - Clicking a conversation row opens the message detail as before.
   - Clicking WhatsApp in sidebar filters to WhatsApp messages.
   - Clicking Inbox clears the selected channel filter and shows all.
   - Inbox sidebar button must not become unresponsive after selecting a channel.
   - Active state should reflect whether user is in Inbox all-view or a specific channel filter.

Likely files:
- artifacts/unboks/src/pages/Inbox.tsx
- artifacts/unboks/src/components/inbox/Drawer.tsx
- artifacts/unboks/src/components/inbox/MessageRow.tsx
- artifacts/unboks/src/hooks/use-platform-filter.tsx
- artifacts/unboks/src/lib/channel-map.ts
- artifacts/unboks/src/lib/conversation-mapper.ts
- artifacts/unboks/src/data/conversations.ts

Investigation:
- Inspect how selected channel/filter is stored.
- Inspect how Drawer sets activeNav / active channel.
- Inspect how Inbox clears or applies channel filter.
- Inspect whether filtering compares raw backend platform to normalized channel label.
- Ensure all filtering uses normalized channel values from platformToChannel.
- Check whether Unknown is being excluded by the filter.
- Check whether Inbox click resets the channel filter.

Important:
The Python backend may return platform/channel values inconsistently:
- whatsapp
- WhatsApp
- whatsapp_business
- wa
- null/empty
Frontend must normalize defensively.
If the backend returns null but these are actually WhatsApp conversations, do not guess Email. If there is another field that indicates WhatsApp, use it. If no channel info exists, label Unknown and keep in Inbox only.

Acceptance:
- The 3 current WhatsApp conversations appear under Inbox.
- The same 3 current WhatsApp conversations appear under WhatsApp when WhatsApp is clicked.
- Email count/list does not include WhatsApp messages.
- Inbox click always resets to all conversations.
- Sidebar active state is correct:
  - Inbox active for all conversations
  - WhatsApp active when WhatsApp filter selected
- Each WhatsApp conversation row shows a small WhatsApp icon/label.
- Each non-WhatsApp conversation row shows correct source label.
- Unknown/missing platform does not show as Email.
- Build/typecheck clean.
- Report exact files changed.
- Do not commit.
- Do not publish.