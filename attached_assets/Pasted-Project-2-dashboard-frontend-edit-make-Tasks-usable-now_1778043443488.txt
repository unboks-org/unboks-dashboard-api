Project 2 dashboard frontend edit — make Tasks usable now with local pending tasks and future sync.

Important current architecture:
- Project 2 Replit is frontend only.
- api.unboks.org points to the canonical Python backend on Vultr.
- Do not modify artifacts/api-server.
- Do not modify backend logic, DNS, secrets, deployment, or Python API assumptions.
- Work only in artifacts/unboks frontend.
- Do not touch Project 1 or Project 3.
- Do not commit.
- Do not publish until tested.

Current issue:
The /tasks page is live but shows:
“Couldn't load tasks: Tasks backend not available yet. Ask the API team to ship /api/unboks/tasks.”

This blocks Calvin from adding tasks now.

Required behavior:
Make /tasks functional now as a local pending task board, while keeping it ready to sync to the real backend once Jr connects /api/unboks/tasks.

Important product rule:
Be honest in the UI.
Do not pretend local tasks are already shared.
Clearly label local-only tasks as:
“Pending sync”
or:
“Saved locally — will sync when backend is connected.”

Goal:
1. Calvin can add tasks now.
2. Tasks persist after refresh in the same browser.
3. Tasks can include pasted screenshots if already supported.
4. Once backend becomes available, the app should sync pending local tasks to the backend.
5. After sync, tasks should appear as normal shared tasks.
6. Jr will see them after backend sync.

Storage:
- Use localStorage for text tasks if simplest.
- If screenshots are supported and too large for localStorage, use IndexedDB or keep screenshots as local previews with a warning.
- Do not store huge images in localStorage if it risks breaking the app.
- For v1, screenshot support may be limited with a clear warning if needed.

UI behavior:
When backend is unavailable:
- Do not show a blocking red error only.
- Show a calm notice:
  “Tasks backend is not connected yet. You can add tasks now; they will be saved locally and synced when the backend is ready.”
- Show composer normally.
- Let user create tasks.
- Show local tasks in the list with a small badge:
  “Pending sync”
- Done/Reopen should work locally.
- Deleting/removing is optional.

When backend becomes available:
- Fetch shared backend tasks.
- Detect local pending tasks.
- Show a button:
  “Sync pending tasks”
- Also optionally auto-sync on page load if safe.
- After successful sync:
  - remove local pending copy
  - show synced backend task
  - badge disappears
- If sync fails:
  - keep local copy
  - show “Sync failed — try again”

Data model for local pending task:
{
  localId: string,
  bodyHtml: string,
  bodyText: string,
  createdBy: "Calvin",
  assignedTo: "Calvin" | "Jr",
  status: "open" | "done",
  attachments: [],
  createdAt: string,
  updatedAt: string,
  completedAt?: string,
  completedBy?: "Calvin",
  syncStatus: "pending" | "syncing" | "failed"
}

Backend API contract remains:
GET /api/unboks/tasks
POST /api/unboks/tasks
PATCH /api/unboks/tasks/:id

Implementation:
- Update the Tasks page and tasks API/client only.
- Do not change Settings.
- Do not change Inbox.
- Do not change dashboard auth.
- Do not change backend.

Likely files:
- artifacts/unboks/src/pages/Tasks.tsx
- artifacts/unboks/src/lib/tasks-api.ts
- artifacts/unboks/src/components/tasks/TaskComposer.tsx
- artifacts/unboks/src/components/tasks/TaskCard.tsx
Possibly add:
- artifacts/unboks/src/hooks/use-local-pending-tasks.ts

Acceptance:
- /tasks loads even when backend returns 404/not available.
- Calvin can add a task while backend is unavailable.
- Added local task appears immediately.
- Local task persists after refresh.
- Local task clearly says “Pending sync”.
- Done/Reopen works locally.
- UI clearly explains Jr will see it only after backend sync.
- If backend later becomes available, pending tasks can be synced.
- No backend changes.
- Build/typecheck clean.
- Report exact files changed.
- Do not commit.
- Do not publish.